#include <iostream>

int main(){
	int n = 0;
	std::cout << "Enter a number between 1 and 100" << std::endl;
	std::cin >> n;
	std::cout << "Wow, " << n << " is my favourite number!" << std::endl;
	return 0;

	// int x = 3;
	// bool b = true;
	// uint y = 4;
	// short z = 3;
	// ushort w = 1;

	// size_t p = sizeof(x);
	// p = sizeof(b);
	// p = sizeof(y);
	// p = sizeof(z);
	// p = sizeof(w);
}